import 'package:flutter/material.dart';

class Matche extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Pantalla de Búsqueda',
        style: TextStyle(fontSize: 24.0),
      ),
    );
  }
}
